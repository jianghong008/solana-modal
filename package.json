{
  "name": "solana-modal",
  "version": "0.0.1",
  "description": "solana-modal",
  "workspaces": [
    "core",
    "packages/solana-modal"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=core",
    "demo": "npm run demo --workspace=core",
    "publish": "powershell ../scripts/publish.ps1"
  }
}
